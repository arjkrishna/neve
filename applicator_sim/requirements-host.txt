# Host (Windows, python 3.13.9) packages used by the host scripts, versions as measured 2026-09-11.
# The SOFA side runs in the docker image eve-training-fixed (SOFA v22.12 + SofaPython3, python 3.8, numpy/scipy);
# run_docker.sh records the image id in every run's cfg.json / summary.json (provenance).
numpy==2.4.2
scipy==1.17.1
nibabel==5.4.2
scikit-image==0.26.0
vtk==9.7.0
matplotlib==3.10.8
